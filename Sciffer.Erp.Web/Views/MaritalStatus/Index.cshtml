@model IEnumerable<Sciffer.Erp.Domain.Model.REF_MARITAL_STATUS>

@{
    ViewBag.Title = "Index";
}
<p>
    <a href="@Url.Action("Create", "MaritalStatus")" data-toggle="modal" class="btn-circle btn-lg btn-primary"><span class="glyphicon glyphicon-plus"></span> </a>
</p>
<div class="row">

    <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="x_panel">
            <table id="example" class="table table-striped responsive-utilities jambo_table table-bordered">
                <thead>
                    <tr class="headings">
                        <th>
                            @Html.DisplayNameFor(model => model.MARITAL_STATUS_NAME)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.MARITAL_STATUS_NAME)
                            </td>
                            <td>
                                @Html.ActionLink("Edit", "Edit", new { id = item.MARITAL_STATUS_ID }) 
                                @Html.ActionLink("Details", "Details", new { id = item.MARITAL_STATUS_ID }) 
                                @Html.ActionLink("Delete", "Delete", new { id = item.MARITAL_STATUS_ID })
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>