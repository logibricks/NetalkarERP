@model IEnumerable<Sciffer.Erp.Domain.Model.REF_TDS_SECTION>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    <a href="@Url.Action("Create", "TDS_Section")" data-toggle="modal" class="btn-circle btn-lg btn-primary"><span class="glyphicon glyphicon-plus"></span> </a>
</p>

<div class="row">

    <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="x_panel">
            <table id="example" class="table table-striped responsive-utilities jambo_table table-bordered">
                <thead>
                    <tr class="headings">
                        <th>
                            @Html.DisplayNameFor(model => model.TDS_SECTION_NAME)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr class="even pointer">
                            <td>
                                @Html.DisplayFor(modelItem => item.TDS_SECTION_NAME)
                            </td>
                            <td>
                                @Html.ActionLink("Edit", "Edit", new { id = item.TDS_SECTION_ID }) |
                                @Html.ActionLink("Details", "Details", new { id = item.TDS_SECTION_ID }) |
                                @Html.ActionLink("Delete", "Delete", new { id = item.TDS_SECTION_ID })
                            </td>
                        </tr>
                    }
                </tbody>

            </table>

        </div>
    </div>
</div>
